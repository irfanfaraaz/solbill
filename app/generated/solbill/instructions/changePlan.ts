/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { SOLBILL_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CHANGE_PLAN_DISCRIMINATOR = new Uint8Array([
  75, 206, 141, 79, 34, 245, 125, 189,
]);

export function getChangePlanDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CHANGE_PLAN_DISCRIMINATOR);
}

export type ChangePlanInstruction<
  TProgram extends string = typeof SOLBILL_PROGRAM_ADDRESS,
  TAccountSubscriber extends string | AccountMeta<string> = string,
  TAccountService extends string | AccountMeta<string> = string,
  TAccountOldPlan extends string | AccountMeta<string> = string,
  TAccountNewPlan extends string | AccountMeta<string> = string,
  TAccountSubscription extends string | AccountMeta<string> = string,
  TAccountSubscriberTokenAccount extends string | AccountMeta<string> = string,
  TAccountDelegate extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSubscriber extends string
        ? ReadonlySignerAccount<TAccountSubscriber> &
            AccountSignerMeta<TAccountSubscriber>
        : TAccountSubscriber,
      TAccountService extends string
        ? ReadonlyAccount<TAccountService>
        : TAccountService,
      TAccountOldPlan extends string
        ? ReadonlyAccount<TAccountOldPlan>
        : TAccountOldPlan,
      TAccountNewPlan extends string
        ? ReadonlyAccount<TAccountNewPlan>
        : TAccountNewPlan,
      TAccountSubscription extends string
        ? WritableAccount<TAccountSubscription>
        : TAccountSubscription,
      TAccountSubscriberTokenAccount extends string
        ? WritableAccount<TAccountSubscriberTokenAccount>
        : TAccountSubscriberTokenAccount,
      TAccountDelegate extends string
        ? ReadonlyAccount<TAccountDelegate>
        : TAccountDelegate,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ChangePlanInstructionData = { discriminator: ReadonlyUint8Array };

export type ChangePlanInstructionDataArgs = {};

export function getChangePlanInstructionDataEncoder(): FixedSizeEncoder<ChangePlanInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CHANGE_PLAN_DISCRIMINATOR }),
  );
}

export function getChangePlanInstructionDataDecoder(): FixedSizeDecoder<ChangePlanInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getChangePlanInstructionDataCodec(): FixedSizeCodec<
  ChangePlanInstructionDataArgs,
  ChangePlanInstructionData
> {
  return combineCodec(
    getChangePlanInstructionDataEncoder(),
    getChangePlanInstructionDataDecoder(),
  );
}

export type ChangePlanAsyncInput<
  TAccountSubscriber extends string = string,
  TAccountService extends string = string,
  TAccountOldPlan extends string = string,
  TAccountNewPlan extends string = string,
  TAccountSubscription extends string = string,
  TAccountSubscriberTokenAccount extends string = string,
  TAccountDelegate extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  subscriber: TransactionSigner<TAccountSubscriber>;
  service: Address<TAccountService>;
  /** The old plan (validated via subscription.plan). */
  oldPlan: Address<TAccountOldPlan>;
  /** The new plan to switch to. */
  newPlan: Address<TAccountNewPlan>;
  subscription?: Address<TAccountSubscription>;
  /** The subscriber's token account. */
  subscriberTokenAccount: Address<TAccountSubscriberTokenAccount>;
  delegate?: Address<TAccountDelegate>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getChangePlanInstructionAsync<
  TAccountSubscriber extends string,
  TAccountService extends string,
  TAccountOldPlan extends string,
  TAccountNewPlan extends string,
  TAccountSubscription extends string,
  TAccountSubscriberTokenAccount extends string,
  TAccountDelegate extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLBILL_PROGRAM_ADDRESS,
>(
  input: ChangePlanAsyncInput<
    TAccountSubscriber,
    TAccountService,
    TAccountOldPlan,
    TAccountNewPlan,
    TAccountSubscription,
    TAccountSubscriberTokenAccount,
    TAccountDelegate,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ChangePlanInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountService,
    TAccountOldPlan,
    TAccountNewPlan,
    TAccountSubscription,
    TAccountSubscriberTokenAccount,
    TAccountDelegate,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLBILL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    subscriber: { value: input.subscriber ?? null, isWritable: false },
    service: { value: input.service ?? null, isWritable: false },
    oldPlan: { value: input.oldPlan ?? null, isWritable: false },
    newPlan: { value: input.newPlan ?? null, isWritable: false },
    subscription: { value: input.subscription ?? null, isWritable: true },
    subscriberTokenAccount: {
      value: input.subscriberTokenAccount ?? null,
      isWritable: true,
    },
    delegate: { value: input.delegate ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.subscription.value) {
    accounts.subscription.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            115, 117, 98, 115, 99, 114, 105, 112, 116, 105, 111, 110,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.subscriber.value)),
        getAddressEncoder().encode(expectAddress(accounts.oldPlan.value)),
      ],
    });
  }
  if (!accounts.delegate.value) {
    accounts.delegate.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            115, 117, 98, 115, 99, 114, 105, 112, 116, 105, 111, 110,
          ]),
        ),
        getAddressEncoder().encode(expectAddress(accounts.subscriber.value)),
        getAddressEncoder().encode(expectAddress(accounts.oldPlan.value)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.subscriber),
      getAccountMeta(accounts.service),
      getAccountMeta(accounts.oldPlan),
      getAccountMeta(accounts.newPlan),
      getAccountMeta(accounts.subscription),
      getAccountMeta(accounts.subscriberTokenAccount),
      getAccountMeta(accounts.delegate),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getChangePlanInstructionDataEncoder().encode({}),
    programAddress,
  } as ChangePlanInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountService,
    TAccountOldPlan,
    TAccountNewPlan,
    TAccountSubscription,
    TAccountSubscriberTokenAccount,
    TAccountDelegate,
    TAccountTokenProgram
  >);
}

export type ChangePlanInput<
  TAccountSubscriber extends string = string,
  TAccountService extends string = string,
  TAccountOldPlan extends string = string,
  TAccountNewPlan extends string = string,
  TAccountSubscription extends string = string,
  TAccountSubscriberTokenAccount extends string = string,
  TAccountDelegate extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  subscriber: TransactionSigner<TAccountSubscriber>;
  service: Address<TAccountService>;
  /** The old plan (validated via subscription.plan). */
  oldPlan: Address<TAccountOldPlan>;
  /** The new plan to switch to. */
  newPlan: Address<TAccountNewPlan>;
  subscription: Address<TAccountSubscription>;
  /** The subscriber's token account. */
  subscriberTokenAccount: Address<TAccountSubscriberTokenAccount>;
  delegate: Address<TAccountDelegate>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getChangePlanInstruction<
  TAccountSubscriber extends string,
  TAccountService extends string,
  TAccountOldPlan extends string,
  TAccountNewPlan extends string,
  TAccountSubscription extends string,
  TAccountSubscriberTokenAccount extends string,
  TAccountDelegate extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof SOLBILL_PROGRAM_ADDRESS,
>(
  input: ChangePlanInput<
    TAccountSubscriber,
    TAccountService,
    TAccountOldPlan,
    TAccountNewPlan,
    TAccountSubscription,
    TAccountSubscriberTokenAccount,
    TAccountDelegate,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ChangePlanInstruction<
  TProgramAddress,
  TAccountSubscriber,
  TAccountService,
  TAccountOldPlan,
  TAccountNewPlan,
  TAccountSubscription,
  TAccountSubscriberTokenAccount,
  TAccountDelegate,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SOLBILL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    subscriber: { value: input.subscriber ?? null, isWritable: false },
    service: { value: input.service ?? null, isWritable: false },
    oldPlan: { value: input.oldPlan ?? null, isWritable: false },
    newPlan: { value: input.newPlan ?? null, isWritable: false },
    subscription: { value: input.subscription ?? null, isWritable: true },
    subscriberTokenAccount: {
      value: input.subscriberTokenAccount ?? null,
      isWritable: true,
    },
    delegate: { value: input.delegate ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.subscriber),
      getAccountMeta(accounts.service),
      getAccountMeta(accounts.oldPlan),
      getAccountMeta(accounts.newPlan),
      getAccountMeta(accounts.subscription),
      getAccountMeta(accounts.subscriberTokenAccount),
      getAccountMeta(accounts.delegate),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getChangePlanInstructionDataEncoder().encode({}),
    programAddress,
  } as ChangePlanInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountService,
    TAccountOldPlan,
    TAccountNewPlan,
    TAccountSubscription,
    TAccountSubscriberTokenAccount,
    TAccountDelegate,
    TAccountTokenProgram
  >);
}

export type ParsedChangePlanInstruction<
  TProgram extends string = typeof SOLBILL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    subscriber: TAccountMetas[0];
    service: TAccountMetas[1];
    /** The old plan (validated via subscription.plan). */
    oldPlan: TAccountMetas[2];
    /** The new plan to switch to. */
    newPlan: TAccountMetas[3];
    subscription: TAccountMetas[4];
    /** The subscriber's token account. */
    subscriberTokenAccount: TAccountMetas[5];
    delegate: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
  };
  data: ChangePlanInstructionData;
};

export function parseChangePlanInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedChangePlanInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      subscriber: getNextAccount(),
      service: getNextAccount(),
      oldPlan: getNextAccount(),
      newPlan: getNextAccount(),
      subscription: getNextAccount(),
      subscriberTokenAccount: getNextAccount(),
      delegate: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getChangePlanInstructionDataDecoder().decode(instruction.data),
  };
}
